---
interface Feature {
  name: string;
  kliento: boolean;
  apiKeys: boolean;
  jwts: boolean;
  spifee: boolean;
}

interface Props {
  features: Feature[];
}

const { features } = Astro.props;

const headerCellClass = "py-4 px-4 text-center bg-neutral-700";
const rowCellClass = "py-4 px-4 text-center";
const checkmarkClass = "text-green-500";
const crossClass = "text-red-500";
---

<div class="overflow-x-auto">
  <table class="w-full border-collapse bg-neutral-800 text-white rounded-lg">
    <thead class="bg-neutral-700 text-base font-normal">
      <tr class="border-b border-neutral-600">
        <th class={headerCellClass + " text-left"}>Features</th>
        <th class={headerCellClass + " bg-red-500"}>Kliento</th>
        <th class={headerCellClass}>Opaque API keys</th>
        <th class={headerCellClass}>JWTs</th>
        <th class={headerCellClass}>SPIFEE</th>
      </tr>
    </thead>
    <tbody>
      {
        features.map((feature) => (
          <tr class="border-b border-neutral-700 ">
            <td class="py-4 px-4 text-sm">{feature.name}</td>
            <td class={rowCellClass + " bg-neutral-900 border-b border-neutral-600"}>
              {feature.kliento ? (
                <span class={checkmarkClass}>✓</span>
              ) : (
                <span class={crossClass}>✗</span>
              )}
            </td>
            <td class={rowCellClass}>
              {feature.apiKeys ? (
                <span class={checkmarkClass}>✓</span>
              ) : (
                <span class={crossClass}>✗</span>
              )}
            </td>
            <td class={rowCellClass}>
              {feature.jwts ? (
                <span class={checkmarkClass}>✓</span>
              ) : (
                <span class={crossClass}>✗</span>
              )}
            </td>
            <td class={rowCellClass}>
              {feature.spifee ? (
                <span class={checkmarkClass}>✓</span>
              ) : (
                <span class={crossClass}>✗</span>
              )}
            </td>
          </tr>
        ))
      }
    </tbody>
  </table>
</div>
